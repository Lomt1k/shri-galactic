# Сервис межгалактической аналитики 🚀

## Описание проекта
Сервис межгалактической аналитики, реализованный на React с использованием TypeScript и современных практик разработки.

Приложение выдаёт аналитику для таблиц с межгалактической статистикой, поддерживает отображение промежуточных результатов обработки, генерацию новых таблиц и хранение истории в LocalStorage.

---

## Архитектура
- **FSD (Feature-Sliced Design)** — модульная организация кода (в упрощённом варианте)
- **Технологии:**
  - TypeScript
  - Vite
  - React + react-router-dom
  - Zustand для управления состоянием
  - CSS Modules
  - Fetch API
  - React Portals для модальных окон
- **Качество кода:** ESLint + Prettier

---

## Функционал
- Загрузка таблиц через drag&drop или кнопку
- Отображение прогресса обработки
- Хранение истории загрузок в LocalStorage
- Генерация новых таблиц
- Навигация между страницами
- Модальные окна с аналитикой из истории

---

## Установка и запуск

### Установка зависимостей (по отдельности в папках `client` и `server`)
```bash
npm install
```

### Запуск сервера
```bash
cd server
npm run start
```

### Запуск клиента
```bash
cd client
npm run dev
```

### Сборка проекта
```bash
cd client
npm run build
```

### Запуск собранной версии
```bash
npm run preview
```

---

## Дополнительно
Все кнопки и ссылки имеют стили для `hover` и `focus-visible`.  
Интерфейс соответствует утвержденным [макетам](https://www.figma.com/design/9gsmAtcuxaZviXSt34yb2a/%D0%94%D0%97-%D0%A8%D0%A0%D0%98?node-id=0-1&p=f&t=8mL2bTYlktWvNTSz-0) с минимальными допустимыми отклонениями.

## Тесты

Приложение содержит в себе набор авто-тестов на основе функциональных требований.

- **Vitest**: 76 тестов (модульные + интеграционные)
- **Playwright**: 2 теста (e2e)

#### Запуск тестов Vitest с графической оболочкой
```bash
cd client
npm run test:ui
```

#### Запуск тестов Vitest без графической оболочки
```bash
cd client
npm run test
```

#### Запуск тестов Playwright с графической оболочкой
```bash
cd client
npm run playwright:ui
```

#### Запуск тестов Playwright без графической оболочки
```bash
cd client
npm run playwright
```

### Функциональные требования, покрытые тестами:
- Загрузка таблиц с данными и получение аналитики
- Отображение прогресса обработки (по требованию пользователей)
- Генератор тестовых данных для проверки системы
- История загрузок (ключевой запрос от клиентов), хранимая в LocalStorage
- Навигационное меню для перемещения между разделами

Ниже вы увидите подробности о покрытии каждого функционального требования.

### Навигационное меню для перемещения между разделами

**e2e тест**: 
- navigation.test.ts - открывает каждую ссылку из nav панели и проверяет переходы между страницами

### Генератор тестовых данных для проверки системы

Данный функционал покрыт как с помощью e2e теста, так и с помощью других.

**e2e тест**: 
- generate.test.ts - открывает страницу генерации, жмет на кнопку генерации, дожидается скачивания и проверяет скаченный файл

**Другие тесты**:

Для того чтобы воспользоваться фичей генерации таблиц, пользователь соприкасается с различными компонентами приложения, которые обложены тестами:

**Компонент Generator**
- ✅ рендерит основной интерфейс и текст
- ✅ отображает кнопку "Начать генерацию" при начальном состоянии
- ✅ при клике на кнопку вызывает fetchReport и отображает UploadButton
- ✅ показывает "файл сгенерирован!" после успешной загрузки
- ✅ вызывает скачивание файла (downloadBlob) с полученным blob и именем файла
- ✅ показывает ошибку при неудачном запросе

**Функция downloadBlob:**
- ✅ корректно создаёт ссылку и запускает скачивание

**API fetchReport:**
- ✅ возвращает Blob при корректном ответе
- ✅ вызывает validateResponse
- ✅ пробрасывает ошибку из validateResponse
- ✅ выбрасывает ошибку, если blob() вернул undefined

**API validateResponse**:
- ✅ не выбрасывает исключение при корректном Response
- ✅ выбрасывает исключение с текстом ошибки от сервера
- ✅ выбрасывает стандартную ошибку при отсутствии текста ошибки от сервера

### Генератор тестовых данных для проверки системы и отображение прогресса обработки

Для того чтобы воспользоваться фичей аналитики, пользователь соприкасается с различными компонентами приложения, которые обложены тестами:

**Компонент UploadForm**:
- ✅ рендерится и отображает кнопку загрузки
- ✅ отображает сообщение об ошибке при загрузке неподходящего файла
- ✅ отображает сообщение "Файл загружен" при загрузке CSV
- ✅ при клике на кнопку "Отправить" вызывается fetchAggregate
- ✅ отображает статистику после успешной загрузки
- ✅ сохраняет статистику в **историю**
- ✅ отображает ошибку при провале fetchAggregate
- ✅ отображает сообщение "Файл загружен" при загрузке CSV файла через Drag N Drop
- ✅ отображает сообщение об ошибке при загрузке неподходящего файла через Drag N Drop

**Компонент UploadButton**:
- ✅ по умолчанию рендерится с текстом "Загрузить файл"
- ✅ если прокинут пропс isLoading, то отображается лоадер
- ✅ если прокинут текст в пропс message, то это сообщение отображается под кнопкой
- ✅ если прокинут текст в пропс result, то он отображается на кнопке
- ✅ при нажатии на кнопку сброса вызывается пропс onReset
- ✅ если прокинут пропс result, то кнопка неактивна
- ✅ если прокинут пропс isLoading, то кнопка неактивна
- ✅ при клике по кнопке симулируется клик по файловому инпуту
- ✅ при выборе файла вызывается пропс onFileChanged

**API fetchAggregate**:
- ✅ корректно обрабатывает успешный поток с одним JSON
- ✅ **корректно обрабатывает несколько сообщений в потоке**
- ✅ корректно обрабатывает частичные строки
- ✅ выбрасывает ошибку при невалидном JSON
- ✅ выбрасывает ошибку при пустом потоке
- ✅ выбрасывает ошибку при отсутствии response.body
- ✅ пробрасывает ошибку из validateResponse (о нём уже сказано ранее)

Для этой фичи пользователь также соприкасается с компонентами **StatisticCard** и **StatisticCardList**, но о тестах данных компонентов я расскажу чуть ниже.

### История загрузок, хранимая в LocalStorage

Для того чтобы воспользоваться фичей истории, пользователь соприкасается с различными компонентами приложения, которые обложены тестами:

**Компонент History**:
- ✅ рендерит секцию и контейнер
- ✅ отображает сообщение "История пуста", если логов нет
- ✅ не отображает кнопку "Очистить всё", если логов нет
- ✅ рендерит список истории, если есть логи
- ✅ отображает кнопку "Очистить всё", если есть логи
- ✅ вызывает clear в сторе при клике на кнопку "Очистить всё"
- ✅ отображает ссылку "Сгенерировать больше"

**Компонент HistoryRow**:
- ✅ корректно отрисовывает данные при наличии статистики
- ✅ корректно отрисовывает данные при отсутствии статистики
- ✅ при клике на содержимое открывает модальное окно с данными
- ✅ при отсутствии статистики открывает пустое модальное окно
- ✅ при клике на кнопку удаления вызывает удаление из стора

**HistoryStore**:
- ✅ вызывает saveToStorage при добавлении записи
- ✅ удаляет запись по ID через remove
- ✅ очищает все записи через clear

**Компонент Modal**:
- ✅ рендерит содержимое внутри portal
- ✅ вызывает onClickClose при клике на фон
- ✅ вызывает onClickClose при клике на кнопку закрытия
- ✅ не вызывает onClickClose при клике на контент

**Компонент StatisticCardList**:
- ✅ рендерит все карточки статистики из prepareRows
- ✅ передаёт правильные пропсы value и description в StatisticCard
- ✅ рендерит только обязательные поля, если опциональные отсутствуют

**Компонент StatisticCard**:
- ✅ отображает значение и описание корректно

**Функция prepareRows**:
- ✅ возвращает правильные строки данных при наличии всех опциональных полей
- ✅ возвращает строки без опциональных полей, если они отсутствуют
- ✅ округляет числа до целых

**Есть и некоторые другие тесты, завязанные на утилитарные функции или переиспользуемые UI-компоненты, с которыми вы можете ознакомиться самостоятельно.**
